function displayArrayElements(arr) {
    for (let i = 0; i < arr.length; i++) {
      console.log(arr[i]);
    }
  }
  
 
  let myArray = ["red", "blue", "orange", "pink"];
  displayArrayElements(myArray);
console.log(displayArrayElements(myArray));
